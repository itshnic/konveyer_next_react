import { Libre_Franklin, Open_Sans, Mulish } from "next/font/google";
export const libreFranklin = Libre_Franklin({
  subsets: ["latin"],
	weight:['400','600','700']
});

export const openSans = Open_Sans({
  subsets: ["latin"],
	weight:['400','600','700']
});

export const mulish = Mulish({
  subsets: ["latin"],
	weight:['400','600','700']
});